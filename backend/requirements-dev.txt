# 开发与测试依赖（在 requirements.txt 之外额外安装）
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# 说明：httpx 是 starlette TestClient 的必需依赖，而它只是 fastapi 的
# optional extra（fastapi[standard]），因此必须在此显式声明，否则
# tests/test_profile.py 会在收集阶段报 "requires the httpx package"。
pytest>=8.0.0
httpx>=0.27.0
